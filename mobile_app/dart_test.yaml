# Живые тесты (сетевые запросы к проду) по умолчанию не запускаются:
# офлайн-сборка и CI не должны падать из-за отсутствия сети.
# Запуск вручную: flutter test --tags live
tags:
  live:
    skip: "сетевой тест: запускать явно через --tags live"
